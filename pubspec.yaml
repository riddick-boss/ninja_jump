name: ninja_jump
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  very_good_analysis: ^4.0.0+1
  shared_preferences: ^2.1.1
  flutter_launcher_icons: ^0.13.1
  change_app_package_name: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/graphics/

flutter_icons:
  image_path_android: "assets/graphics/ninja.png"
  adaptive_icon_background: "#00ffffff"
  adaptive_icon_foreground: "assets/graphics/ninja.png"
  android: true